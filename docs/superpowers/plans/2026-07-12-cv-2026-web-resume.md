# CV 2026 Web Resume Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Build a clean, A4-like web resume site in `/Users/dylan/Documents/Projects/cv-2026` using only the content from `CV - 2026.md`.

**Architecture:** A Vite + React + TypeScript single-page app renders structured resume data from `src/resumeData.ts`. Styling lives in one focused stylesheet with separate screen, mobile, and print rules. GitHub Pages deployment is handled by a workflow that builds `dist` and publishes it as a Pages artifact.

**Tech Stack:** React 18, TypeScript, Vite, pnpm, CSS, GitHub Actions, GitHub Pages.

## Global Constraints

- Use only `/Users/dylan/Documents/Projects/doc-repo/40 - Archive/Personal/CV/CV - 2026.md` as resume content source.
- Publicly show the contact information exactly as written in the resume.
- Do not use `/Users/dylan/Documents/Projects/doc-repo/40 - Archive/Personal/CV/项目总结 - 2026.md`.
- Keep the site concise and resume-like, not portfolio-like.
- Do not modify `/Users/dylan/Documents/Projects/doc-repo`.
- Do not create git commits unless explicitly requested.
- Source project root is `/Users/dylan/Documents/Projects/cv-2026`.

---

## File Structure

- `package.json`: npm scripts and dependencies.
- `index.html`: Vite HTML entry.
- `vite.config.ts`: React plugin and GitHub Pages base path.
- `tsconfig.json`, `tsconfig.node.json`: TypeScript settings.
- `.gitignore`: local build artifacts and dependencies.
- `.github/workflows/deploy.yml`: GitHub Pages deployment.
- `README.md`: local development, PDF export, and deployment instructions.
- `src/main.tsx`: React bootstrap.
- `src/App.tsx`: render the resume page and toolbar.
- `src/resumeData.ts`: structured resume data copied from `CV - 2026.md`.
- `src/styles.css`: visual, responsive, and print CSS.

---

### Task 1: Scaffold Static React Project

**Files:**
- Create: `/Users/dylan/Documents/Projects/cv-2026/package.json`
- Create: `/Users/dylan/Documents/Projects/cv-2026/index.html`
- Create: `/Users/dylan/Documents/Projects/cv-2026/vite.config.ts`
- Create: `/Users/dylan/Documents/Projects/cv-2026/tsconfig.json`
- Create: `/Users/dylan/Documents/Projects/cv-2026/tsconfig.node.json`
- Create: `/Users/dylan/Documents/Projects/cv-2026/.gitignore`

**Interfaces:**
- Produces: `pnpm dev`, `pnpm build`, `pnpm preview`.
- Consumes: none.

- [ ] **Step 1: Create `package.json`**

```json
{
  "name": "cv-2026",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "@vitejs/plugin-react": "^4.3.4",
    "typescript": "^5.7.2",
    "vite": "^6.0.5",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@types/react": "^18.3.18",
    "@types/react-dom": "^18.3.5"
  }
}
```

- [ ] **Step 2: Create the Vite entry files**

`index.html`:

```html
<!doctype html>
<html lang="zh-CN">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta name="description" content="黎思奇的 2026 Web 简历" />
    <title>黎思奇 | Web Resume 2026</title>
  </head>
  <body>
    <div id="root"></div>
    <script type="module" src="/src/main.tsx"></script>
  </body>
</html>
```

`vite.config.ts`:

```ts
import { defineConfig } from 'vite';
import react from '@vitejs/plugin-react';

export default defineConfig({
  base: '/cv-2026/',
  plugins: [react()],
});
```

- [ ] **Step 3: Create TypeScript config**

`tsconfig.json`:

```json
{
  "files": [],
  "references": [
    { "path": "./tsconfig.node.json" },
    { "path": "./tsconfig.app.json" }
  ]
}
```

Also create `tsconfig.app.json`:

```json
{
  "compilerOptions": {
    "target": "ES2020",
    "useDefineForClassFields": true,
    "lib": ["DOM", "DOM.Iterable", "ES2020"],
    "allowJs": false,
    "skipLibCheck": true,
    "esModuleInterop": true,
    "allowSyntheticDefaultImports": true,
    "strict": true,
    "forceConsistentCasingInFileNames": true,
    "module": "ESNext",
    "moduleResolution": "Bundler",
    "resolveJsonModule": true,
    "isolatedModules": true,
    "noEmit": true,
    "jsx": "react-jsx"
  },
  "include": ["src"]
}
```

`tsconfig.node.json`:

```json
{
  "compilerOptions": {
    "composite": true,
    "skipLibCheck": true,
    "module": "ESNext",
    "moduleResolution": "Bundler",
    "allowSyntheticDefaultImports": true,
    "strict": true
  },
  "include": ["vite.config.ts"]
}
```

- [ ] **Step 4: Create `.gitignore`**

```gitignore
node_modules
dist
.DS_Store
*.local
```

- [ ] **Step 5: Verify scaffold compiles after later source tasks**

Run after Task 3: `pnpm build`
Expected: TypeScript and Vite build complete successfully.

---

### Task 2: Model Resume Content

**Files:**
- Create: `/Users/dylan/Documents/Projects/cv-2026/src/resumeData.ts`

**Interfaces:**
- Produces: `resume`, exported as `ResumeData`.
- Consumes: only content from `CV - 2026.md`.

- [ ] **Step 1: Define resume types and data**

Create `src/resumeData.ts` with types for header, summaries, skill groups, experience items, education, and links.

- [ ] **Step 2: Copy content from `CV - 2026.md`**

Include the name, metadata, target role, contact, location/languages, summary bullets, skill groups, Shopee and Baidu experience, education, GitHub, Juejin, Npm, open-source link, and self-media note.

- [ ] **Step 3: Keep data concise**

Do not add AMS details from `项目总结 - 2026.md`; keep the Shopee AMS text at the detail level present in `CV - 2026.md`.

---

### Task 3: Render Resume UI

**Files:**
- Create: `/Users/dylan/Documents/Projects/cv-2026/src/main.tsx`
- Create: `/Users/dylan/Documents/Projects/cv-2026/src/App.tsx`
- Create: `/Users/dylan/Documents/Projects/cv-2026/src/styles.css`

**Interfaces:**
- Consumes: `resume` from `src/resumeData.ts`.
- Produces: a single printable resume page with a toolbar print button.

- [ ] **Step 1: Bootstrap React**

`src/main.tsx` should import React, ReactDOM, `App`, and `styles.css`, then mount into `#root`.

- [ ] **Step 2: Render page structure**

`src/App.tsx` should render:
- toolbar with `window.print()`
- resume sheet
- header
- summary
- skills
- work experience
- education
- other links

- [ ] **Step 3: Use accessible links**

External links should use `target="_blank"` and `rel="noreferrer"`.

---

### Task 4: Add A4-like Responsive and Print Styling

**Files:**
- Modify: `/Users/dylan/Documents/Projects/cv-2026/src/styles.css`

**Interfaces:**
- Consumes: class names from `App.tsx`.
- Produces: screen, mobile, and print layouts.

- [ ] **Step 1: Add desktop screen layout**

Use a light gray app background, centered white resume sheet, subtle border, and A4-like max width.

- [ ] **Step 2: Add mobile layout**

At `max-width: 720px`, remove A4 fixed width, reduce padding, and keep sections single-column.

- [ ] **Step 3: Add print layout**

Use `@page { size: A4; margin: 12mm; }`, hide `.toolbar`, remove background and shadow, and avoid page breaks inside section blocks where practical.

---

### Task 5: Add Docs and GitHub Pages Deployment

**Files:**
- Create: `/Users/dylan/Documents/Projects/cv-2026/.github/workflows/deploy.yml`
- Create: `/Users/dylan/Documents/Projects/cv-2026/README.md`

**Interfaces:**
- Consumes: `pnpm build` from `package.json`.
- Produces: GitHub Pages deployment workflow and usage docs.

- [ ] **Step 1: Create GitHub Pages workflow**

Workflow should:
- trigger on pushes to `main`
- install pnpm
- run `pnpm install --frozen-lockfile`
- run `pnpm build`
- upload `dist`
- deploy to Pages

- [ ] **Step 2: Create README**

README should include:
- `pnpm install`
- `pnpm dev`
- `pnpm build`
- `pnpm preview`
- PDF export through browser print
- GitHub Pages setup note

---

### Task 6: Validate Locally

**Files:**
- Read: `/Users/dylan/Documents/Projects/cv-2026/package.json`
- Read: `/Users/dylan/Documents/Projects/cv-2026/dist/index.html`

**Interfaces:**
- Consumes: all previous tasks.
- Produces: verified build output.

- [ ] **Step 1: Install dependencies**

Run: `pnpm install`
Expected: dependencies install and `pnpm-lock.yaml` is created.

- [ ] **Step 2: Build**

Run: `pnpm build`
Expected: TypeScript and Vite complete without errors and `dist` is created.

- [ ] **Step 3: Start dev server**

Run: `pnpm dev`
Expected: local Vite URL is shown.

- [ ] **Step 4: Inspect responsive behavior**

Open the local URL and inspect desktop width and mobile width. The resume should remain readable without overlapping text.

- [ ] **Step 5: Inspect print behavior**

Use browser print preview. The toolbar should be hidden and the resume content should print on A4 pages.

---

## Self-Review Notes

- Spec coverage: content scope, A4-like layout, mobile layout, print/PDF export, GitHub Pages deployment, and no `doc-repo` modifications are covered.
- Placeholder scan: no unfinished markers are present.
- Type consistency: data is exported as `resume` and consumed by `App.tsx`.
